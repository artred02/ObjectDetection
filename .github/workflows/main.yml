name: .NET Core CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Vérifier le code source
        uses: actions/checkout@v2
        run: |
          $globalJson = Get-Content -Raw -Path global.json | ConvertFrom-Json
          $SDK_VERSION = $globalJson.sdk.version
          echo "SDK version found: $SDK_VERSION"
          echo "SDK_VERSION=$SDK_VERSION" >> $env:GITHUB_ENV  # Définir la version dans les variables d'environnement
          
      - name: Configurer la version de .NET SDK
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: ${{ env.SDK_VERSION }}

      - name: Configurer la version de .NET SDK
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: ${{ env.SDK_VERSION }}

      - name: Restaurer les dépendances
        run: dotnet restore

      - name: Exécuter les tests unitaires
        run: dotnet test

      - name: Publier le projet en format exécutable autonome
        run: dotnet publish -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true

      - name: Archiver les artefacts (optionnel)
        uses: actions/upload-artifact@v3 
        with:
          name: ObjectDetection
          path: ./bin/Release/net6.0/win-x64/publish/ObjectDetection.exe
